<form class="form-horizontal">
  <fieldset>
    <legend>Edit Post: {{title}}</legend>

    {{partial "posts/form"}}
    
    <div class="form-actions">
      <button type="submit" class="btn btn-primary" {{action 'update'}} {{bindAttr disabled=notDirty}}>Save changes</button>
    </div>
  </fieldset>
</form>